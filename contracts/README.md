# contracts/

The entire enforcement policy, ~100 lines of YAML. Loaded by `src/pb/enforce/contracts.py`.

`policy.yaml` is a **draft** (generic role-level rules, no tool-specific overrides yet) — not reviewed, not frozen. See its header comment and `SPEC.md` "Open questions."

Frozen alongside `src/pb/enforce/` after week 3 — see CLAUDE.md rule 3.
