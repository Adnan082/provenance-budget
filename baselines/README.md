# baselines/

Published defences run as-shipped, never reimplemented — see CLAUDE.md rule 5.

- `progent/` — git submodule of sunblaze-ucb/progent, pinned to a commit.
- `camel/` — git submodule of google-research/camel-prompt-injection, pinned to a commit.

If a baseline can't be made to run, it's omitted and the omission is recorded in RESULTS.md — not silently dropped.
