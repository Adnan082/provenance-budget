# traces/test/ — SEALED

Do not read, run against, or tune on this split before week 6. It runs exactly once, via `make test-final`.

If a command would touch this directory outside `make test-final`, stop and ask — see CLAUDE.md rule 2.
